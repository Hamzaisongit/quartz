- react is a frontend lib which uses extention of js called jsx allowing us to easily compose js and html in one place
- uses react fiber and virtual dom to reduce overhead processing 
- attribute convention : camelCase
- necessarily single-self enclosing tags must br as <tag /> ie.. <br />
	- have a look in this btw
- extending React.component and using it's render methode
- way1 for components : RecatDOM.render(JSX,where)
- way2 : normal functional comps
- props in react
	- default props
	- proptypes (good practice)
	- when accessing props inside component created using class.. use this.props.
- stateless functional components, stateless components and stateful components
- state: an object inside stateful comps
	- this.state inside constructor
	- any the properties defined will cause rerendering is changed keeps track of such properties or data
	- however state obj is encapsulated to the hosting component and all it's child comps where state data/property is passed as a prop,...  when this data changes only the associated components will be rerendered using the virtualDOM 
- setState :
	- a methodw peovided by React.Component
	- https://www.freecodecamp.org/news/what-is-state-in-react-explained-with-examples/
	- direct setting and based on previous states and previous props setting
- handling input states and creating controlled input forms elements
	- onChange attr of input ele
	- set input property of state obj to event.target.value
	- set value attr of input eleement to the input property of state obj
- passing states as props..in downward direction in comp hierarchy
	- howere complex app.. there'll be one or two stateful components and from them state properties will be passed on to child comps if any needs it
	- this flow is always unidirectional..top-bottom and one of the key paradigms in React
	- ensures a single source of truth
- u can also pass functions and all as props too
- lifecycles methodes that could be called at specific times during lifecycle of the component
	- componentWillMount :before mounted on DOM
	- componentDidMount : after mounted on DOM
	- componentWillUnmount: for cleanup before removing a comp..good practice
	- shouldComponentUpdate(nextProp,nextState) : returen true or false and accordingly it will be decided the componebt should be rerendered or nor if val of prop or state is changed 
	- ComponentDidUpdate: what to do if rerendered
- you can include regular/usual javascript inside render methode before return and use variabes directly in JSX without 
- conditional rendering or displaying of elements using {condition && element}
	- ternary and AND combined when strictly nothing is to be done even before checking the ternary condition :{this.state.userAge && (this.state.userAge<18?buttonThree:buttonTwo)}
- using map and return from it's callBack to render elements
- setting key value to all identical sibling elements for optimized React
- 





#### Pending
- mutable and states of normal dom and how React integrates with it
- binding and contextualizing functions using this keyword inside constructors
- Why even react: [Why is React dramatically increasing in use and popularity (by 311.3%)? What features/advantages does this tech have over existing JavaScript UI libraries to spark a major increase in attention by the Stack Overflow community? - Quora](https://www.quora.com/Why-is-React-dramatically-increasing-in-use-and-popularity-by-311-3-What-features-advantages-does-this-tech-have-over-existing-JavaScript-UI-libraries-to-spark-a-major-increase-in-attention-by-the-Stack-Overflow-community)
- 