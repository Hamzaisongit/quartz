- reduce methode (acc call back and initial value)- mdn
- split methdoe - mdn
- sort methode 
- using ternary operator to make binary decision making way more compact
- Object.values() & Object.keys() - retruns array of respective
- Set object - non repetative
- accessing keys of obj through variables.. obj[var]
- trick to find highest valued key from keys array: 
	-  ```    const highest = Object.keys(counts).sort((a, b) => counts[b]  - counts[a])[0];
- bad practice to mutate the parameters..: for arrays use splice to create a shallow copy of array and then applied sort

